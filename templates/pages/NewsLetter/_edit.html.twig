{% block body %}
    <div class="container text-left text-show">
        <div class="p-3">
            {{  include ('pages/NewsLetter/_form.html.twig', {form: form, button: 'Editer'}) }}
            <br/>
        </div>
    </div>
{% endblock %}